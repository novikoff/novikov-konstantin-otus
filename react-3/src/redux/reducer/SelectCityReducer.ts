import {IActionIncDec,Actions} from '../action/actions';
const initialState = {
    counter: 1,
    select_cities_list:[],
    full_list:[],
    dropDownFlag:false,
}

export default function SelectCityReducer(state = initialState, action:IActionIncDec) {
    switch(action.type){

            
    }
    return state
  }